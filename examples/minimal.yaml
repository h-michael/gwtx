# yaml-language-server: $schema=https://raw.githubusercontent.com/h-michael/kabu/main/schema/kabu.schema.json

# Minimal kabu configuration
# Copy this file to .kabu/config.yaml in your repository root to get started.

# This is the simplest possible configuration with no operations.
# kabu will create worktrees without any additional setup.
# You can add operations as needed.

# Uncomment to set global conflict handling
# defaults:
#   on_conflict: backup  # abort, skip, overwrite, backup
